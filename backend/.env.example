# CivicGrant IQ — Environment Variables
# Copy this file to .env and fill in your values from the Azure portal deployment outputs

# ── Mock Mode (zero credentials) ─────────────────────────────────────────
# Set to "true" to run the full pipeline via the Tier 3 deterministic mock engine.
# The pipeline completes in <200ms with no Azure credentials required.
# Perfect for offline demos, CI environments, and hackathon presentations.
# FORCE_MOCK_MODE=true

# ── Azure Application Insights (civicgrant-insights) ────────────────────
APPLICATIONINSIGHTS_CONNECTION_STRING=InstrumentationKey=<your-instrumentation-key>;IngestionEndpoint=https://eastus2-3.in.applicationinsights.azure.com/;LiveEndpoint=https://eastus2.livediagnostics.monitor.azure.com/;ApplicationId=<your-application-id>

# ── Azure AI Search Free tier (civicgrant-srch) ───────────────────────────────
SEARCH_ENDPOINT=https://civicgrant-srch.search.windows.net
SEARCH_API_KEY=<your-search-admin-key>
KNOWLEDGE_BASE_NAME=civicgrant-kb

# ── Azure OpenAI ─────────────────────────────────────────────────────────
AOAI_ENDPOINT=https://<your-openai-service>.openai.azure.com
AOAI_API_KEY=<your-azure-openai-api-key>
AOAI_GPT_DEPLOYMENT=gpt-4o-mini
AOAI_EMBEDDING_DEPLOYMENT=text-embedding-3-large

# ── Microsoft Foundry Project ─────────────────────────────────────────────
# Format: https://<hub>.services.ai.azure.com/api/projects/<project-name>
FOUNDRY_PROJECT_ENDPOINT=https://<your-hub>.services.ai.azure.com/api/projects/<your-project>
FOUNDRY_MODEL_DEPLOYMENT=gpt-4o-mini

# ── Azure Blob Storage (Municipal Docs Corpus) ────────────────────────────
BLOB_CONNECTION_STRING=DefaultEndpointsProtocol=https;AccountName=<account>;AccountKey=<key>;EndpointSuffix=core.windows.net
BLOB_CONTAINER_NAME=civicgrant-docs

# ── Microsoft Graph / SharePoint Work IQ Context ─────────────────────────
GRAPH_TENANT_ID=<your-directory-tenant-id>
GRAPH_CLIENT_ID=<your-application-client-id>
GRAPH_CLIENT_SECRET=<your-client-secret-value>
GRAPH_SITE_HOSTNAME=<your-sharepoint-hostname>   # e.g. contoso.sharepoint.com
GRAPH_SITE_PATH=<your-site-path>                 # e.g. /sites/CityClerk
GRAPH_LIBRARY_NAME=<your-library-name>           # e.g. City Grant Intelligence
# UPN of the M365 user whose calendar + mailbox Work IQ reads for live signals
# Required app permissions: Calendars.Read, Mail.Read, ChannelMessage.Read.All
GRAPH_USER_UPN=<user@yourdomain.com>

# ── Azure Resource Details ────────────────────────────────────────────────
AZURE_SUBSCRIPTION_ID=<your-subscription-id>
AZURE_RESOURCE_GROUP=rg-skillsfest
AZURE_LOCATION=eastus2

# ── Server ────────────────────────────────────────────────────────────────
PORT=3001
